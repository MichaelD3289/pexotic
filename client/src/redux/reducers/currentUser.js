// constant type variables

// action functions


// reducer function
export default function currentUserReducer(state={}, action) {
  switch (action.type) {
    // case statements

    // default statement
    default:
      return state;
  }
}